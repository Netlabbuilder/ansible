# Inventory file is a custom 'hosts' file, not the default ansible 'hosts' file 
# List all managed nodes in inventory 'hosts' file
  ansible all -i inventory --list-hosts

# Check connections to all (submarine ping, not ICMP)
  ansible all -i hosts -m ping

# Run a command (here is "uptime") on all the hosts in the 'rocky1' group
  ansible rocky1 -i hosts -m command -a "uptime"

# Collect and display known facts for “rocky2” group
  ansible rocky2 -i hosts -m setup
